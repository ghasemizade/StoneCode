let firstNumber = Number(prompt("enter first number:"), 1)
let secondNumber = Number(prompt("enter second number:"), 1)

const Pow = (a, b) => {
    return a ** b;
}


console.log(Pow(firstNumber, secondNumber));
